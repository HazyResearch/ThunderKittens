namespace warp {

#include "memory/memory.impl"
#include "register/register.impl"

void tests(test_data &results) {
    std::cout << "\n\n --------------- Starting ops/warp tests! ---------------\n\n" << std::endl;
    memory::tests(results);
    reg::tests(results); // register is a reserved word
}

}