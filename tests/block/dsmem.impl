#define DSMEM_TEST_WORKERS       4

template<st_layout layout, int height, int width, int cs>
__global__ void __cluster_dims__(cs, 1, 1) 
test_nextneighbor_ker(const bf16 *input, bf16 *output) {
    auto warpid        = kittens::warpid();
    auto lane          = kittens::laneid(); 

    const int workers  = DSMEM_TEST_WORKERS;

    extern __shared__ int __shm[];
    shared_allocator al = shared_allocator::create_allocator((int*)&__shm[0]); 

    st_bf<height, width, layout> (&input_tile)[workers]  = al.allocate<st_bf<height, width, layout>, workers>();
    st_bf<height, width, layout> (&output_tile)[workers] = al.allocate<st_bf<height, width, layout>, workers>();

    namespace cg = cooperative_groups;
    cg::cluster_group cluster = cg::this_cluster();
    unsigned int cluster_size = cluster.num_blocks();
    unsigned int cluster_idx = blockIdx.x % cluster_size;

    const bf16* block_start = input + (cluster_idx * (input_tile[0].num_elements * workers));

    auto block = cooperative_groups::this_thread_block();

    load(input_tile[warpid], block_start + (warpid * input_tile[0].num_elements), input_tile[0].cols);

    // // PRINT Statement to see what's being loaded into each threadblock
    // __syncthreads(); 
    // if (threadIdx.x == 0 && cluster_idx == 1) {
    //     // print out input from global memory
    //     printf("input_tile:\n"); 
    //     for (int w = 0; w < workers; w++) {
    //         for (int r = 0; r < input_tile[0].rows; r++) {
    //             for (int c = 0; c < input_tile[0].cols; c++) {
    //                 printf("%f ", __bfloat162float(input_tile[w].data[r * input_tile[0].cols + c]));
    //             }
    //             printf("\n");
    //         }
    //         printf("\n");
    //     }
    //     printf("\n");
    // }
    // __syncthreads();

    __shared__ uint64_t smem_barrier[1];
    constexpr int size_bytes = sizeof(bf16) * input_tile[0].num_elements * workers; 
    dsmem::init_barrier(smem_barrier[0], 1); 
    dsmem::set_barrier_bytes(smem_barrier[0], size_bytes);

    cluster.sync();

    int neighbor_idx = (cluster_idx + 1) % cluster_size;
    dsmem::tile_distribute_smem(output_tile[warpid], input_tile[warpid], cluster_size, neighbor_idx, size_bytes, smem_barrier[0]);

    constexpr int kPhaseBit = 0;
    dsmem::distribution_wait(smem_barrier[0], kPhaseBit);

    cluster.sync();

    // // PRINT Statement to see what tile distribute smem did
    // __syncthreads(); 
    // if (threadIdx.x == 0 && cluster_idx == 2) {
    //     // print out output_tile
    //     printf("output_tile:\n"); 
    //     for (int w = 0; w < workers; w++) {
    //         for (int r = 0; r < output_tile[0].rows; r++) {
    //             for (int c = 0; c < output_tile[0].cols; c++) {
    //                 printf("%f ", __bfloat162float(output_tile[w].data[r * output_tile[0].cols + c]));
    //             }
    //             printf("\n");
    //         }
    //         printf("\n");
    //     }
    //     printf("\n");
    // }
    // __syncthreads();

    // write out the results from output_tile to global memory
    bf16* output_block_start = output + (cluster_idx * output_tile[0].num_elements * workers);
    store(output_block_start + (warpid * output_tile[0].num_elements), output_tile[warpid], output_tile[0].cols);
}
template<st_layout layout, int height, int width, int cluster_size>
bool test_nextneighbor() {
    // input for each threadblock = 8 x 4 (8 workes, 1x4 tile) shared tile filled with cluster_idx
    // output for each threadblock = 8 x 4 (8 workes, 1x4 tile) filled with (cluster_idx - 1) % cluster_size

    int workers         = DSMEM_TEST_WORKERS; 
    int threads         = DSMEM_TEST_WORKERS * WARP_SIZE; 
    int tile_elements   = (16 * height) * (16 * width) * workers; 
    int total_elements  = tile_elements * cluster_size; 

    bf16 *d_i, *d_o; 
    std::vector<float> i_ref(total_elements);
    std::vector<float> o_ref(total_elements);
    // input for cluster_idx n = n
    for (int i = 0; i < total_elements; i++) {
        i_ref[i] = i/tile_elements;
    }
    initialize<false>(&d_i, &d_o, i_ref, o_ref);

    unsigned long mem_size = (tile_elements * 2 * workers * sizeof(bf16)); 
    mem_size += 1024; // for the barrier and safety
    cudaFuncSetAttribute(test_nextneighbor_ker<layout, height, width, cluster_size>, cudaFuncAttributeMaxDynamicSharedMemorySize, mem_size);
    cudaFuncSetAttribute(test_nextneighbor_ker<layout, height, width, cluster_size>, cudaFuncAttributeNonPortableClusterSizeAllowed, cluster_size);
    CudaCheckError();

    // run kernel
    test_nextneighbor_ker<layout, height, width, cluster_size><<<cluster_size, threads, mem_size>>>(d_i, d_o);
    CudaCheckError();

    for (int i = 0; i < tile_elements; i++) {
        o_ref[i] = cluster_size - 1;
    }
    for (int i = tile_elements; i < total_elements; i++) {
        o_ref[i] = static_cast<int>(i_ref[i] - 1) % cluster_size;
    }

    bool passed = validate(d_i, d_o, i_ref, o_ref, "dsmem_nextneighbor_" + std::to_string(height) + "x" + std::to_string(width) + "_" + layout_name<layout>() + "_" + std::to_string(cluster_size));

    return passed;
}

template<st_layout layout, int height, int width, int cluster_size>
bool test_nextneighbor_rand() {
    // input for each threadblock = 8 x 4 (8 workes, 1x4 tile) shared tile filled with cluster_idx
    // output for each threadblock = 8 x 4 (8 workes, 1x4 tile) filled with (cluster_idx - 1) % cluster_size

    int workers         = DSMEM_TEST_WORKERS; 
    int threads         = DSMEM_TEST_WORKERS * WARP_SIZE; 
    int tile_elements   = (16 * height) * (16 * width) * workers; 
    int total_elements  = tile_elements * cluster_size; 

    bf16 *d_i, *d_o; 
    std::vector<float> i_ref(total_elements);
    std::vector<float> o_ref(total_elements);

    initialize(&d_i, &d_o, i_ref, o_ref);

    unsigned long mem_size = (tile_elements * 2 * workers * sizeof(bf16)); 
    mem_size += 1024; // for the barrier and safety
    cudaFuncSetAttribute(test_nextneighbor_ker<layout, height, width, cluster_size>, cudaFuncAttributeMaxDynamicSharedMemorySize, mem_size);
    cudaFuncSetAttribute(test_nextneighbor_ker<layout, height, width, cluster_size>, cudaFuncAttributeNonPortableClusterSizeAllowed, cluster_size);
    CudaCheckError();

    // run kernel
    test_nextneighbor_ker<layout, height, width, cluster_size><<<cluster_size, threads, mem_size>>>(d_i, d_o);
    CudaCheckError();

    // cudaMemcpy(i_ref.data(), d_i, total_elements * sizeof(bf16), cudaMemcpyDeviceToHost);
    // cudaMemcpy(o_ref.data(), d_o, total_elements * sizeof(bf16), cudaMemcpyDeviceToHost);

    // for (int i = 0; i < total_elements; i++) {
    //     printf("i_ref[%d]: %f\n", i, __bfloat162float(i_ref[i]));
    // }

    // for (int i = 0; i < total_elements; i++) {
    //     printf("o_ref[%d]: %f\n", i, __bfloat162float(o_ref[i]));
    // }

    for (int i = 0; i < tile_elements; i++) {
        o_ref[i] = i_ref[(tile_elements) * (cluster_size - 1) + i];
    }
    for (int i = tile_elements; i < total_elements; i++) {
        o_ref[i] = i_ref[i - tile_elements];
    }

    // for (int i = 0; i < tile_elements; i++) {
    //     o_ref[i] = cluster_size - 1;
    // }
    // for (int i = tile_elements; i < total_elements; i++) {
    //     o_ref[i] = static_cast<int>(i_ref[i] - 1) % cluster_size;
    // }
    
    bool passed = validate(d_i, d_o, i_ref, o_ref, "dsmem_nextneighbor_" + std::to_string(height) + "x" + std::to_string(width) + "_" + layout_name<layout>() + "_" + std::to_string(cluster_size));
    return passed; 
}

int dsmem_tests() {
    std::cout << " ----- Starting dsmem tests! -----" << std::endl;
    int failures = 0;

    // Cluster size 16 (rand)
    failures += !test_nextneighbor_rand<st_naive_row_layout, 1, 1, 16>();
    failures += !test_nextneighbor_rand<st_xor_row_layout, 1, 1, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_row_0b_layout, 1, 1, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_row_32b_layout, 1, 1, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_col_t_0b_layout, 1, 1, 16>();

    failures += !test_nextneighbor_rand<st_naive_row_layout, 1, 2, 16>();
    failures += !test_nextneighbor_rand<st_xor_row_layout, 1, 2, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_row_0b_layout, 1, 2, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_row_32b_layout, 1, 2, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_col_t_0b_layout, 1, 2, 16>();

    failures += !test_nextneighbor_rand<st_naive_row_layout, 1, 3, 16>();
    failures += !test_nextneighbor_rand<st_xor_row_layout, 1, 3, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_row_0b_layout, 1, 3, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_row_32b_layout, 1, 3, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_col_t_0b_layout, 1, 3, 16>();

    failures += !test_nextneighbor_rand<st_naive_row_layout, 1, 4, 16>();
    failures += !test_nextneighbor_rand<st_xor_row_layout, 1, 4, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_row_0b_layout, 1, 4, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_row_32b_layout, 1, 4, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_col_t_0b_layout, 1, 4, 16>();

    failures += !test_nextneighbor_rand<st_naive_row_layout, 2, 1, 16>();
    failures += !test_nextneighbor_rand<st_xor_row_layout, 2, 1, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_row_0b_layout, 2, 1, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_row_32b_layout, 2, 1, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_col_t_0b_layout, 2, 1, 16>();

    failures += !test_nextneighbor_rand<st_naive_row_layout, 2, 2, 16>();
    failures += !test_nextneighbor_rand<st_xor_row_layout, 2, 2, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_row_0b_layout, 2, 2, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_row_32b_layout, 2, 2, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_col_t_0b_layout, 2, 2, 16>();

    failures += !test_nextneighbor_rand<st_naive_row_layout, 2, 3, 16>();
    failures += !test_nextneighbor_rand<st_xor_row_layout, 2, 3, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_row_0b_layout, 2, 3, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_row_32b_layout, 2, 3, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_col_t_0b_layout, 2, 3, 16>();

    failures += !test_nextneighbor_rand<st_naive_row_layout, 2, 4, 16>();
    failures += !test_nextneighbor_rand<st_xor_row_layout, 2, 4, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_row_0b_layout, 2, 4, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_row_32b_layout, 2, 4, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_col_t_0b_layout, 2, 4, 16>();

    failures += !test_nextneighbor_rand<st_naive_row_layout, 4, 1, 16>();
    failures += !test_nextneighbor_rand<st_xor_row_layout, 4, 1, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_row_0b_layout, 4, 1, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_row_32b_layout, 4, 1, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_col_t_0b_layout, 4, 1, 16>();

    failures += !test_nextneighbor_rand<st_naive_row_layout, 4, 2, 16>();
    failures += !test_nextneighbor_rand<st_xor_row_layout, 4, 2, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_row_0b_layout, 4, 2, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_row_32b_layout, 4, 2, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_col_t_0b_layout, 4, 2, 16>();

    failures += !test_nextneighbor_rand<st_naive_row_layout, 4, 3, 16>();
    failures += !test_nextneighbor_rand<st_xor_row_layout, 4, 3, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_row_0b_layout, 4, 3, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_row_32b_layout, 4, 3, 16>();
    failures += !test_nextneighbor_rand<st_wgmma_col_t_0b_layout, 4, 3, 16>();

    // Cluster size 16 (range)
    failures += !test_nextneighbor<st_naive_row_layout, 1, 1, 16>();
    failures += !test_nextneighbor<st_xor_row_layout, 1, 1, 16>();
    failures += !test_nextneighbor<st_wgmma_row_0b_layout, 1, 1, 16>();
    failures += !test_nextneighbor<st_wgmma_row_32b_layout, 1, 1, 16>();
    failures += !test_nextneighbor<st_wgmma_col_t_0b_layout, 1, 1, 16>();

    failures += !test_nextneighbor<st_naive_row_layout, 1, 2, 16>();
    failures += !test_nextneighbor<st_xor_row_layout, 1, 2, 16>();
    failures += !test_nextneighbor<st_wgmma_row_0b_layout, 1, 2, 16>();
    failures += !test_nextneighbor<st_wgmma_row_32b_layout, 1, 2, 16>();
    failures += !test_nextneighbor<st_wgmma_col_t_0b_layout, 1, 2, 16>();

    failures += !test_nextneighbor<st_naive_row_layout, 1, 3, 16>();
    failures += !test_nextneighbor<st_xor_row_layout, 1, 3, 16>();
    failures += !test_nextneighbor<st_wgmma_row_0b_layout, 1, 3, 16>();
    failures += !test_nextneighbor<st_wgmma_row_32b_layout, 1, 3, 16>();
    failures += !test_nextneighbor<st_wgmma_col_t_0b_layout, 1, 3, 16>();

    failures += !test_nextneighbor<st_naive_row_layout, 1, 4, 16>();
    failures += !test_nextneighbor<st_xor_row_layout, 1, 4, 16>();
    failures += !test_nextneighbor<st_wgmma_row_0b_layout, 1, 4, 16>();
    failures += !test_nextneighbor<st_wgmma_row_32b_layout, 1, 4, 16>();
    failures += !test_nextneighbor<st_wgmma_col_t_0b_layout, 1, 4, 16>();

    failures += !test_nextneighbor<st_naive_row_layout, 2, 1, 16>();
    failures += !test_nextneighbor<st_xor_row_layout, 2, 1, 16>();
    failures += !test_nextneighbor<st_wgmma_row_0b_layout, 2, 1, 16>();
    failures += !test_nextneighbor<st_wgmma_row_32b_layout, 2, 1, 16>();
    failures += !test_nextneighbor<st_wgmma_col_t_0b_layout, 2, 1, 16>();

    failures += !test_nextneighbor<st_naive_row_layout, 2, 2, 16>();
    failures += !test_nextneighbor<st_xor_row_layout, 2, 2, 16>();
    failures += !test_nextneighbor<st_wgmma_row_0b_layout, 2, 2, 16>();
    failures += !test_nextneighbor<st_wgmma_row_32b_layout, 2, 2, 16>();
    failures += !test_nextneighbor<st_wgmma_col_t_0b_layout, 2, 2, 16>();

    failures += !test_nextneighbor<st_naive_row_layout, 2, 3, 16>();
    failures += !test_nextneighbor<st_xor_row_layout, 2, 3, 16>();
    failures += !test_nextneighbor<st_wgmma_row_0b_layout, 2, 3, 16>();
    failures += !test_nextneighbor<st_wgmma_row_32b_layout, 2, 3, 16>();
    failures += !test_nextneighbor<st_wgmma_col_t_0b_layout, 2, 3, 16>();

    failures += !test_nextneighbor<st_naive_row_layout, 2, 4, 16>();
    failures += !test_nextneighbor<st_xor_row_layout, 2, 4, 16>();
    failures += !test_nextneighbor<st_wgmma_row_0b_layout, 2, 4, 16>();
    failures += !test_nextneighbor<st_wgmma_row_32b_layout, 2, 4, 16>();
    failures += !test_nextneighbor<st_wgmma_col_t_0b_layout, 2, 4, 16>();

    failures += !test_nextneighbor<st_naive_row_layout, 4, 1, 16>();
    failures += !test_nextneighbor<st_xor_row_layout, 4, 1, 16>();
    failures += !test_nextneighbor<st_wgmma_row_0b_layout, 4, 1, 16>();
    failures += !test_nextneighbor<st_wgmma_row_32b_layout, 4, 1, 16>();
    failures += !test_nextneighbor<st_wgmma_col_t_0b_layout, 4, 1, 16>();

    failures += !test_nextneighbor<st_naive_row_layout, 4, 2, 16>();
    failures += !test_nextneighbor<st_xor_row_layout, 4, 2, 16>();
    failures += !test_nextneighbor<st_wgmma_row_0b_layout, 4, 2, 16>();
    failures += !test_nextneighbor<st_wgmma_row_32b_layout, 4, 2, 16>();
    failures += !test_nextneighbor<st_wgmma_col_t_0b_layout, 4, 2, 16>();

    failures += !test_nextneighbor<st_naive_row_layout, 4, 3, 16>();
    failures += !test_nextneighbor<st_xor_row_layout, 4, 3, 16>();
    failures += !test_nextneighbor<st_wgmma_row_0b_layout, 4, 3, 16>();
    failures += !test_nextneighbor<st_wgmma_row_32b_layout, 4, 3, 16>();
    failures += !test_nextneighbor<st_wgmma_col_t_0b_layout, 4, 3, 16>();

    return failures;
}